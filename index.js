const divide = function(a, b) {
    return a / b;
};

// Arrow function called square
const square = (num) => {
    return num * num;
};

// Arrow function called add
const add = (x, y) => {
    return x + y;
};